services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/data/tagesmappe.db
    volumes:
      - db-data:/data
    restart: unless-stopped

volumes:
  db-data:
